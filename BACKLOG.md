# Backlog

Dated one-liners for everything deferred or spotted and not done. Format: `date · source · status — item`.

- 2026-09-25 · publish times · open — Three launch posts went live on 2026-09-23 but are dated earlier, so they carry 00:00 UTC: `welcome-to-aitamer`, `open-weights-roundup`, `policy-watch-transparency`. An editor should set the real time or correct the date.
- 2026-09-25 · publish times · open — The bot pipeline must run `npm run stamp` before committing a published post, and commit the specimen ledger with it, or the checks stop at `check:posts` (on the pull request, and again in the deploy).
- 2026-09-25 · contact form · done 2026-09-25 — the branch re-allocated to 0.1.5 when it merged.
- 2026-09-25 · archive · open — A month page lists every story that month; paginate it (and the desk and author pages) before a month holds a few hundred posts. Number archive pages oldest-first so old pages stay cached.
- 2026-09-25 · pipeline · idea — The posts MCP (new / update / remove) should implement `POST.md` exactly: immutable slug, `npm run stamp` on publish, `updatedDate` on edit, hero JPEG at `public/heroes/<slug>.jpg`.
- 2026-09-25 · CI · idea — Add Dependabot for GitHub Actions and npm so version bumps arrive as pull requests instead of by hand.
- 2026-09-25 · contact form · open — Turn on Turnstile: create a widget for aitamer.news, set `TURNSTILE_SITE_KEY` in `src/lib/site.ts` and the Worker secret `TURNSTILE_SECRET_KEY` together. The code is ready and tested.
- 2026-09-25 · contact form · done 2026-09-26 (first deploy accepted both bindings) — Confirm on the first deploy that the free plan accepts the Workers rate-limit binding. If not, add a WAF rate-limiting rule on `contact.aitamer.news` and revisit the fail-closed check.
- 2026-09-25 · About page · done 2026-09-25 — `src/content/authors/desk-bot.md` said drafts go "for human review before publish", which didn't match practice. Bio now says the desk's research and legal checks run first, with a human editor reviewing only when confidence is low.
- 2026-09-25 · layout · superseded 2026-09-25 — On a phone the sticky header (nav + ten desk pills on four rows) covers about 45% of the screen. Make the pill row scroll sideways or collapse on small screens. (Big Top layout; the Bestiary theme replaced the pills with a habitat nav and a phone tab bar.)
- 2026-09-25 · contact review · open — Honeypot uses `autocomplete="new-password"`; a password manager might offer to fill it and silently drop a real note. Test with 1Password/Bitwarden/Chrome and pick the attribute that no manager fills.
- 2026-09-25 · contact review · open — Night theme (not shipped): the Publish node's white text on the night rose and `--bot` success text on the night card fail contrast. Fix before ever shipping night.
- 2026-09-25 · specimens · open — Two posts stamped on separate branches can take the same number. Since fix lane F1 (2026-09-25), `check-posts.yml` catches it on the pull request, before it reaches `main`, and POST.md section 4 gives the append-only repair (a `void` ledger line). Still open: issue numbers only on `main`, in one serialised step (a CI job with its own concurrency group, or atn-mcp as the only writer), so collisions cannot happen at all.
- 2026-09-25 · specimens (fix lane F1) · open — A renamed post file has no repair procedure: the check reports "specimen N belongs to old-slug". Define one (for example an append-only rename line in the ledger) before atn-mcp can rename posts.
- 2026-09-25 · CI (fix lane F1) · open — The disabled `deploy-github-pages.yml` still runs `check:times`, not `check:posts`, and still triggers on push. Update it, or delete it if it stays retired.
- 2026-09-25 · Bestiary · open — Fallback cover SVGs (`public/covers/`) still use the Big Top palette; draw night-palette versions. Rarely seen: every post has a hero.
- 2026-09-25 · Bestiary · open — The contact Worker's no-JavaScript error page (`workers/contact/src/handler.mjs`, `plainPage`) still uses the Big Top colours and fonts.
- 2026-09-25 · SEO · open — `/news-sitemap.xml` is as fresh as the last deploy; with no new post for two days it keeps listing old stories until the next build. Harmless for Google, but a daily rebuild (or the scheduled job deploying daily) would keep it exact.
- 2026-09-25 · Bestiary · idea — Morning Leash newsletter and email extinction alerts: need a subscriber store and bulk sending (Michel's approval). Candidates on the free tier: D1 or KV for subscribers behind a Worker; sending through a provider, since Cloudflare Email sends only to verified addresses.
- 2026-09-25 · scale · open — Before ~6,000 posts (about 16,000 deploy files, when `npm run check:files` starts warning): move hero images to R2 (the contract already accepts `https://` hero URLs). Each post costs ~3 files (page, hero, search fragment); see `docs/adr/0005-deploy-file-budget.md`.
- 2026-09-25 · release review · open — Scripted dead-link check over `dist/` (plan task C5): every internal `href` resolves to a built file.
- 2026-09-25 · release review · open — `scripts/decode-heroes.mjs` still runs before every build and dev though no `.b64` sources exist any more; remove the step and the POST.md §3 paragraph, or keep it documented as dormant.
- 2026-09-25 · release review · open — The contact Worker still allow-lists `https://michelabboud.github.io`; remove it when the stale GitHub Pages copy is unpublished.
- 2026-09-25 · release review A · open — `/campfire/` lists every published post with a comment count each, plus the 30 newest comments site-wide; at thousands of posts and comments it becomes a multi-megabyte page. Paginate it with the archive, desk and author pages.
- 2026-09-25 · release review A · open — The footer year (`new Date().getFullYear()` in `BaseLayout.astro`) is frozen into incrementally cached pages; it goes stale on 1 January until a full rebuild. Derive it from `BUILD_TIME` or force a full build on the first deploy of a year.
- 2026-09-25 · release review A · open — GitHub turns off scheduled workflows in a public repository after 60 days without a commit, which would silently stop `scheduled-publish.yml`. Record it in a runbook and watch for the warning email.
- 2026-09-25 · comments (lane A1) · open — The comment contract allows `\n` as the only control character in `text` (phase 2 plan §5.1), while the desk's normalisation keeps tabs. A comment with a tab would fail the public schema at publish time and stay unpublished. Decide on the desk side: turn tabs into spaces before publishing (recommended), or widen the contract additively.
- 2026-09-25 · comments (lane A2) · done (fix lane A2F: the visible text is built from the parsed URL, userinfo and IP hosts are not linked) — A comment link's visible text is what the reader typed, so `https://trusted.example@evil.example/` reads as one host and goes to another (userinfo). The link is `nofollow ugc` and the desk pre-moderates, so this is a moderation concern today; if links ever auto-publish, show the parsed host next to the link or refuse URLs with userinfo on the desk side.
- 2026-09-25 · comments (lane A2) · done (lane A4) — Until Disqus is removed (task A4) a post page shows two comment sections: the site's own (`#comments`) and the Disqus thread below it, titled "Earlier comments, on Disqus". Remove `DisqusThread.astro` with the rest of Disqus.
- 2026-09-25 · comments (lane A3) · superseded by lane A3F (the check keys on `MAINTAINER_ID` now; line below) — Until the repository variable `PUBLISHER_LOGIN` is set (the publisher App's `<slug>[bot]` login), the required check `publisher-paths` holds every pull request to the publisher's rule, so a human pull request that touches code fails it. Fail safe by design; set the variable when the App exists.
- 2026-09-25 · comments (lane A3) · done (lane A3F: `scripts/slug.mjs`, no `npm ci`) — `publisher-paths` runs `npm ci` only so `scripts/check-publisher-paths.mjs` can import `SLUG` from `scripts/frontmatter.mjs` (which needs js-yaml). Inlining the slug pattern, with a test that it still equals `SLUG`, would make the check dependency-free and a minute faster.
- 2026-09-25 · comments (lane A3) · open — The comment form's `maxlength` counts UTF-16 code units while the contract counts code points, so a comment of nearly 2,000 characters with many emoji is stopped by the browser a little early. Harmless; the Worker is the authority.
- 2026-09-25 · comments (lane A3) · done (fix lane AF: `data-action="contact"`, and the contact Worker requires action `contact` on `aitamer.news`) — The comments Worker will require Turnstile action `comment`; the contact Worker checks no action, so a token solved on the comment form could still be spent on the contact form. Give the contact widget `data-action="contact"` and have its Worker require it, for symmetry.
- 2026-09-25 · comments (fix lane A1F) · open — The post contract has the same re-export pattern the comment contract just left: `src/pages/contract/v1/post.schema.json.ts` re-exports the unversioned handler, so `/contract/v1/post.schema.json` carries the unversioned `$id` and nothing freezes v1's bytes. Give it its own `$id` and a golden snapshot like `src/content/comment-schema.v1.snapshot.json` (out of scope for A1F: the post contract's routes were not to be touched).
- 2026-09-25 · comments (fix lane A1F) · open — A validator that compiles the published comment patterns with the `u` flag (ajv's default, @cfworker/json-schema) lets refused characters above U+FFFF through (plane 14, planes 15–16, supplementary noncharacters); no ECMA-262 pattern can express those ranges identically with and without the flag. The build refuses them and the schema description says so. When the desk's publisher picks its validator, have it apply `FORBIDDEN_CHARACTERS.ranges` (or the `unicodeClass`) as well, so a comment with a tag character is refused at publish time rather than on the pull request.
- 2026-09-25 · comments (fix lane A1F) · open — `check:comments` refuses a dotfile in `src/content/comments/`, so a `.DS_Store` written by macOS Finder fails `npm run check:posts` locally until it is deleted. Deliberate (the directory holds a public contract's data and nothing else); if it becomes a nuisance, ignore exactly `.DS_Store` by name, never dotfiles in general.
- 2026-09-25 · comments (lane A3F) · open — Set the repository variable `MAINTAINER_ID` (the maintainer's numeric account id, `gh api users/<login> --jq .id`). Until then `publisher-paths` holds every pull request to the publisher's rule, so a pull request that touches code fails it (fail safe). Also set `PUBLISHER_ACTOR_ID` (the App's numeric bot account id, `gh api 'users/<app-slug>[bot]' --jq .id`; was the login `PUBLISHER_ACTOR` until fix lane AF, 2026-09-26) when the App exists; until then the deploy's publisher push guard skips with a notice.
- 2026-09-25 · comments (lane A3F) · open — Michel's dashboard step: a branch ruleset that confines the publisher App to `desk/comments-*` branches (it may create and push only there, never `main` or another branch), so it cannot push into other pull requests' branches at all. The `publisher-paths` check catches that case today; the ruleset stops it happening.
- 2026-09-25 · comments (lane A3F) · open, unverified — A change to `PUBLIC_COMMENTS_ENDPOINT` (or `COMMENTS_ENDPOINT` in `src/lib/site.ts`) may not invalidate Astro's incremental cache for post pages whose content did not change, so cached pages could keep the old form `action` until a full build. Check whether the incremental build hashes env or `site.ts` changes; if not, force a full build when the endpoint changes.
- 2026-09-25 · comments (lane A3F) · open — The comment form's inline script mirrors `commentErrorText` (`src/lib/comment-form.ts`) rather than importing it (an inline script cannot import); the behaviour was exercised once in a DOM stub, but no committed test runs the shipped script. Add a browser-level test (Playwright over `dist/`) when the site gets one.
- 2026-09-25 · comments (lane A3F) · open — The comments Worker (desk's repository) must redirect a no-script submit to `/posts/<slug>/?commented=1#comment-held` (was `#comments`); POST.md section 8 should say so too, and should drop `PUBLISHER_LOGIN` for `MAINTAINER_ID` / `PUBLISHER_ACTOR`.
- 2026-09-26 · comments (fix lane A2F) · open — No automated test renders `Comments.astro` itself: its logic is in the pure `commentViews` (tested), and Astro escapes the name, but a component-level test needs Astro's container API under Vite (a test runner this repo does not have). Add one with the browser-level tests, or when a Vite-based runner arrives.
- 2026-09-26 · comments (fix lane A2F) · open — "One changed comment file re-renders one post page" (the per-post `cacheKey`) has no automated check: a build here takes about 3 s, but the build log lists every route on every run (two builds with no change both listed all 25 post pages), so the log cannot tell a cached page from a re-rendered one, and the test would have to build a copy of the project to avoid touching the checkout. First find a reliable signal (per-page render output, or the cache's own manifest), then make it a CI job that builds twice around a one-file change and counts the re-rendered pages; if that shows every post re-rendering, the incremental cache is not doing its job and that is a defect of its own.
- 2026-09-26 · comments (fix lane A2F) · open — The desk should apply the same link rules before it publishes (refuse or flag URLs with userinfo or IP-address hosts), so a moderator sees what the page will show; the site no longer depends on it.
- 2026-09-26 · batch A deep review (fix lane AF) · open — Michel's dashboard step (ADR 0007): in the `main` ruleset, require the checks `publisher-paths` and `check` (the `Check posts` job), both with their source set to GitHub Actions; give the publisher App no Commit statuses, Checks or Actions permission; keep your own unfinished pull requests as drafts, since the App can merge any green pull request.
- 2026-09-26 · batch A deep review (fix lane AF) · open — The `Check posts` job's check name is the generic `check`. Before the ruleset requires it, consider giving the job a distinctive `name:` (renaming it after the ruleset exists means editing the ruleset in the same change, or every pull request waits for a check that never reports).
- 2026-09-26 · batch A deep review (fix lane AF) · done 2026-09-26 (0.2.15: `commentFormSetup` requires `contactFormLive`) — Enforce the go-live order in code rather than only in the docs: keep the comment form off (the "not set up yet" note) while `CONTACT_FORM_LIVE` is false, so setting `TURNSTILE_SITE_KEY` first cannot open comments before their removal channel exists.
- 2026-09-26 · batch A deep review (fix lane AF) · open — The comment contract now refuses every format character (Unicode category Cf) above U+FFFF, including the Kaithi number signs U+110BD and U+110CD, which are visible prepended concatenation marks; their Arabic-script counterparts below U+FFFF (U+0600–0605, U+06DD, U+070F, U+0890–0891, U+08E2) stay allowed. Harmless in practice (Kaithi is a historic script), but inconsistent: decide whether to allow the two Kaithi marks again in a later additive change, or refuse the Arabic ones too (that one is breaking: v2).
- 2026-09-26 · batch A deep review (fix lane AF) · open — `check-posts.yml`, `scheduled-publish.yml` and `deploy-github-pages.yml` still use actions by tag (`@v7`, `@v5`); only the workflows that hold the Cloudflare token or run on `pull_request_target` are pinned by commit. Pin the rest the same way for consistency.
- 2026-09-26 · contact form · open — `deploy-contact-worker.yml` deploys with the site's `CLOUDFLARE_API_TOKEN`, which was made for Pages; the first CI deploy after a change to `workers/contact/` may be refused until that token also has Workers Scripts: Edit and Workers custom domains on `aitamer.news`. The first deploy was manual (2026-09-26).
- 2026-09-26 · reactions (task RB1) · open — The reactions contract's `n` is published as `type: integer`, `minimum: 1` **and** `maximum: 9007199254740991`: zod writes the largest exact JSON integer for every `int()`, and the build enforces it. The publisher's own validator, which refuses keywords it does not implement, must implement `maximum` as well as `integer` and `minimum` before it validates reactions files.
- 2026-09-26 · reactions (task RB1) · done 2026-09-26 (0.2.22: `[slug].astro` reads `getReactionThreads()` and its `cacheKey` carries the file's stamp) — `getReactionThreads()` and the `reactions` collection have no reader until the reactions component lands (task RB3); the story page's incremental-build key must gain the reactions file's stamp in that task, or a changed total would not rebuild its page.
- 2026-09-26 · reactions (task RB2) · open — The deploy's guard step is still named "The publisher may only have changed comment files", and its shell's error for a non-push publisher run says "it may only push comment files". Both now mean either lane (ADR 0008); they were left as they are so the widening changed no workflow logic and `scripts/deploy-publisher-guard.test.mjs` (which finds the step by name) ran unchanged. Rename them together with the test's `GUARD_STEP` in a later change of its own.
- 2026-09-26 · reactions (task RB2) · open — The desk's own path check (the publisher's `guardPath`, in the desk's repository) must widen to the same two patterns as `scripts/check-publisher-paths.mjs` before the publisher writes a reactions file (ADR 0008, rule 4).
- 2026-09-26 · reactions (task RB3) · open (narrowed 2026-09-26, 0.2.26) — The component's behaviour is now a bundled module that imports its rules from `src/lib/reactions.ts`, so the unit tests cover the code readers run; the DOM wiring itself (panel, focus, requests) is still exercised only by a hand-run Playwright checklist in Chromium and Firefox (the RB3 fix close-out), not by a committed test. Add it to the browser-level tests when the site gets them.
- 2026-09-26 · reactions (task RB3) · done 2026-09-26 (confirmed by the RB3/RB4 deep review: the Worker's `/react` matches the form type with `startsWith`, so the `charset` parameter is accepted) — The page sends `Content-Type: application/x-www-form-urlencoded;charset=UTF-8` (what a browser sets for a `URLSearchParams` body). The comments Worker's `POST /react` must accept the `charset` parameter on a form type, or every reaction is refused with 415.
- 2026-09-26 · reactions (task RB3) · open — Screen-reader pass (VoiceOver or NVDA) not done: the names and roles were checked in Chromium's accessibility tree ("React" button with `aria-haspopup="menu"`; "Overhyped, 1,204 reactions" `menuitemradio` [checked]); a real screen reader is owed at the go-live review (plan risk R5).
- 2026-09-26 · reactions (task RB3) · open — After a 410 the reader's refused choice stays shown on the (now disabled) button and in the in-session count, as the plan specifies ("410 → the note, the button disabled"). Reverting it would be more honest; decide at the go-live review.
- 2026-09-26 · reactions (task RB3) · open — "A changed reactions file re-renders only its story" is unproven for the same reason as the comments line above (the build log lists every route); the `cacheKey` carries the file's stamp.
- 2026-09-26 · reactions (task RB4) · open — Michel reviews the privacy page's reactions paragraph (`src/pages/privacy.astro`, behind `REACTIONS_LIVE`) before the flag flips: it is a statement to readers. Its "30 days" (and "within a day after", the nightly purge) must stay equal to the desk's reaction retention and to `REACTION_MEMORY_DAYS`; the "Last updated" date moves in the commit that flips the flag.
- 2026-09-26 · reactions (RB3/RB4 deep review) · open — WebKit (Safari) is untested: the reactions component was checked in Chromium and Firefox only, because no WebKit build matching an installed Playwright was available here. Run the browser checklist in WebKit before go-live, or check by hand on an iPhone.
- 2026-09-26 · rendered-body gate (S4) · open — Michel's Q4: move the two YouTube iframes in made-on-youtube-2026-gemini-ask-studio.md to the video: field (it holds one) or keep them; then remove the grandfather entry (`GRANDFATHERED_POSTS` in `scripts/check-rendered-body.mjs`). Any edit to the post voids the entry and fails the build until it is removed.
- 2026-09-26 · rendered-body gate (S4) · open — The checker imports Astro internals by path (`astro/dist/core/config/config.js`, `…/vite-plugin-markdown/content-entry-type.js`, `…/content/mutable-data-store.js`). An Astro upgrade that moves them stops the gate (fails closed); re-point the paths in `scripts/check-rendered-body.mjs` and re-run `npm run check:bodies:build` after every Astro or satteri upgrade.
- 2026-09-26 · rendered-body gate (S4) · open — The allowlist pins Shiki's `github-dark` output and satteri's footnote and task-list shapes. A theme change or a satteri upgrade that changes those shapes fails every bot post with code or footnotes until the allowlist is reviewed; that is intended, and the review goes in `scripts/rendered-body-allowlist.mjs` with a test.
- 2026-09-26 · rendered-body gate (S4) · idea — A tripwire for every post (human ones too) is Michel's Q4; `node scripts/check-rendered-body.mjs --all` already reports it (today: no human post would fail).
- 2026-09-26 · rendered-body gate (S4) · open — `check:bodies:build` compares with the build's data store, which the deploy restores from the incremental-build cache. If an upgrade ever leaves a stale rendered entry there, the comparison fails the deploy (safe direction); the fix is a cache-less build (delete the `astro-incremental-cloudflare-` cache entry).
- 2026-09-26 · rendered-body gate (S4 review G3) · open — Requirement for posts task S1 (the posts lane guard, not built yet): a posts-App pull request may only add or change posts whose new `author` is a bot id (`desk-bot`), and must not change a post whose current author is not a bot; otherwise the gate must check every post that pull request touches, whatever author it claims. The gate trusts the `author` field today (ADR 0009, "What the gate rests on").
