---
interface Props {
  kind: 'human' | 'bot';
  /** Author name, shown after the kind: "BOT · DESK BOT". */
  name?: string;
}
const { kind, name } = Astro.props;
const label = kind === 'bot' ? 'Bot' : 'Human';
---
<span class={`tag tag--${kind}`}>{name ? `${label} · ${name}` : label}</span>
